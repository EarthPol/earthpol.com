---
slug: earthpol-blog-1
title: EarthPol's First Blog Post
authors: [mrtytanic]
tags: [Update, Announcement]
---

import Admonition from '@theme/Admonition';

<!--
📂 **Location**: Place this file under your site’s `blog/` directory.
📅 **Filename**: `YYYY-MM-DD-your-slug.mdx` → Docusaurus parses the date (2025-05-05) automatically.
📝 **Frontmatter**:
  • `slug` – the URL path override (here `/blog/earthpol-blog-1`).
  • `title` – your post’s headline.
  • `authors` – keys matching entries in `blog/authors.yml`.
  • `tags` – array of tags for grouping/filtering.
Optional fields you can also use:
  • `hide_title`, `description`, `image`, `sidebar_label`, `draft`
-->

# Hello, EarthPol Community!

Welcome to **EarthPol’s** very first official blog post.
Below you’ll see examples of how to leverage Docusaurus’s blog features via MDX.

---

## 1. Basic Markdown Content

Write standard Markdown for text, headings, lists, links, etc.

### Lists

- Use `-` or `*` for bullet lists
- Indent for nested lists

### Links

[Point users to docs](https://earthpol.com/docs)

---

## 2. Images

![EarthPol Logo](/img/full-size-logo.webp)

> **Tip**:
> Place assets in `static/` (e.g. `static/img/full-size-logo.webp`), then reference them with a leading `/`.

---

## 3. Admonitions

<Admonition type="info" title="Pro Tip">
    You can import and use the built-in `<Admonition>` component to highlight notes, warnings, etc.
</Admonition>

    ---

    ## 4. Code Blocks

    ```js
    // Identify yourself in the console
    console.log('Welcome to EarthPol!');